#prama once

#include "../../utils/helpers.h"
#include <string>

struct shader_compiler_config
{
    const std::string vertex_shader_path = nullptr;
    const std::string fragment_shader_path = nullptr;
    const std::string output_path = nullptr;
};